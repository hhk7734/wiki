---
id: alacritty
title: Alacritty
sidebar_label: Alacritty
description: Alacritty
keywords:
  - alacritty
---

## 설치

```shell
sudo pacman -S alacritty
```

```shell
curl -sSL https://github.com/alacritty/alacritty/releases/download/v0.13.2/alacritty.info \
    | tic -x -
```

:::info
tic 명령어를 사용할 수 없는 원격 서버에 접속하는 경우 `TERM=xterm-256color ssh ...`와 같은 방식으로 접속해야합니다.
:::

## 설정

- [Alacritty Configuration](https://alacritty.org/config-alacritty.html)

### font

```shell
sudo pacman -S ttf-meslo-nerd
```

```shell
fc-list

...
<path> <font>:style=<style>
...
```

```toml title="~/.config/alacritty/alacritty.toml"
[font]
normal = { family = "MesloLGM Nerd Font", style = "Regular" }
```

### window

```yaml title="~/.config/alacritty/alacritty.yml"
[window]
dimensions = { columns = 120, lines = 50 }
padding = { x = 5, y = 5 }
```

### theme

- https://github.com/alacritty/alacritty-theme

```shell
# We use Alacritty's default Linux config directory as our storage location here.
mkdir -p ~/.config/alacritty/themes
git clone https://github.com/alacritty/alacritty-theme ~/.config/alacritty/themes
```

```toml title="~/.config/alacritty/alacritty.toml"
import = [
  "~/.config/alacritty/themes/themes/github_dark.yaml",
]
```
