---
id: overview
title: "xargs 사용법"
sidebar_label: "xargs 사용법"
description: "xargs 사용법 documents overview material for xargs in the system domain. It is classified as an entity page for the tool class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights xargs, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include shell script, findutils, stdin, xargs 사용법, and system, helping both humans and generated indexes connect this page with adjacent concepts. The route is system / xargs / overview, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - "shell script"
  - xargs
  - findutils
  - stdin
  - "xargs 사용법"
  - system
ontology:
  role: entity
  domain: system
  class: tool
  instance: xargs
  aspect: overview
subject:
  canonical_name: xargs
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## xargs

`xargs`는 GNU Findutils 중 하나입니다.

`stdin`으로 들어오는 문자열을 띄어쓰기 또는 개행으로 구분하여 주어진 명령어의 인자로 전달하여 명령어를 완성 시킨 후 실행합니다.

```shell
<stdout> | xargs [<options>] <command>
```

- `<options>`
  - `-t|--verbose`: 실행할 명령어를 출력합니다.
  - `-n|--max-args <num>`: 한번에 명령어로 전달할 인자의 최대 개수를 지정합니다.
  - `-r|--no-run-if-empty`: stdin이 비어있을 경우 명령어를 실행하지 않습니다.
  - `-I <arg>`: 입력받은 인자로 치환될 수 있는 문자열을 지정합니다.
    - `echo -n a b c | xargs -d " " -I {} echo {}` 명령어는 `echo a`, `echo b`, `echo c`를 실행합니다.

:::warning
`|`는 stdout만 stdin으로 보내기 때문에 stderr를 처리하려면 `2>&1`을 사용해야합니다.

- [Redirection과 Piping](/docs/language/shellscript/redirection-piping.mdx)

:::
