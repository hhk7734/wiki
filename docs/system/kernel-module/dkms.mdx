---
id: dkms
title: DKMS
description: "DKMS documents dkms material for Kernel module in the system domain. It is classified as an operation page for the kernel class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights dkms.conf, DKMS 모듈 리스트, DKMS에 module 추가, and DKMS에서 module 제거, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include kernel, module, build, dkms, and kernel-module, helping both humans and generated indexes connect this page with adjacent concepts. The route is system / kernel-module / dkms, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - kernel
  - module
  - build
  - dkms
  - "kernel module"
  - kernel-module
  - system
ontology:
  role: operation
  domain: system
  class: kernel
  instance: kernel-module
  aspect: dkms
subject:
  canonical_name: "Kernel module"
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
sidebar_label: DKMS
---

## dkms.conf

```shell
<package>
├── dkms.conf
├── Makefile
└── ...
```

```shell title="dkms.conf"
PACKAGE_NAME="<package>"
PACKAGE_VERSION="<version>"
MAKE[0]="make -j$(nproc) KVERSION=$kernelver"
CLEAN="make clean"
BUILT_MODULE_NAME[0]="<module>"
DEST_MODULE_LOCATION[0]="/updates/dkms"
AUTOINSTALL="YES"
```

- `$kernelver`: `dkms` 명령어 사용시 현재 커널 버전 또는 `-k <kernelVersion>`에 의해 대체되는 변수입니다

## DKMS 모듈 리스트

```shell
dkms status
```

## DKMS에 module 추가

```shell
sudo dkms add -m <package> -v <version>
```

`/var/lib/dkms/<package>/<version>/source` -\> `/usr/src/<package>-<version>` 인 심볼릭 링크가 생성됩니다.

:::info
`sudo dkms add <path/package>` 명령어로 추가하는 경우 해당 디렉토리 내의 파일들이 `/usr/src/<package>-<version>`로 복사된 후 위와 같이 `/var/lib/dkms`에 추가됩니다.
:::

```shell
sudo dkms build -m <package> -v <version>
```

`/var/lib/dkms/<package>/<version>/<kernelVersion>/<arch>/module/<module>.ko` 파일이 생성됩니다.

```shell
sudo dkms install -m <package> -v <version>
```

빌드된 모듈 파일을 `/lib/module/<kernelVersion>/updates/dkms/` 에 복사합니다.

```shell
modinfo <module>
```

```shell
modprobe <module>
```

## DKMS에서 module 제거

```shell
sudo dkms remove
```
