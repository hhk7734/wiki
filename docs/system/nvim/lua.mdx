---
id: lua
title: "Neovim Lua 가이드"
sidebar_label: "Lua 가이드"
description: "Neovim Lua 가이드 documents lua material for Neovim in the system domain. It is classified as an operation page for the tool class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights 기본 라이브러리, lua 실행runtimepath, and Options, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include nvim, lua, neovim lua 가이드, and system, helping both humans and generated indexes connect this page with adjacent concepts. The route is system / nvim / lua, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - nvim
  - neovim
  - lua
  - "neovim lua 가이드"
  - system
ontology:
  role: operation
  domain: system
  class: tool
  instance: nvim
  aspect: lua
subject:
  canonical_name: Neovim
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## 기본 라이브러리

```shell
:lua vim.print(package.loaded)
```

```shell
:lua vim.print(vim)
```

[Vim API](https://neovim.io/doc/user/lua.html)와 [Nvim API](https://neovim.io/doc/user/api.html)은 `vim.*`으로 접근할 수 있습니다.

## lua 실행(runtimepath)

:::info[References]

- [Neovim / Options # runtimepath](https://neovim.io/doc/user/options.html#'runtimepath')

:::

```shell
:lua vim.print(vim.opt.runtimepath:get())
```

기본적으로 다음 경로를 확인합니다.

- `$XDG_CONFIG_HOME/nvim`
- `$XDG_DATA_HOME/nvim/site`
- `$VIMRUNTIME`
- `$XDG_DATA_HOME/nvim/site/after`
- `$XDG_CONFIG_HOME/nvim/after`

:::info

- `$XDG_CONFIG_HOME`
  - `~/.config` = `stdpath('config')`
  - `/etc/xdg`
- `$XDG_DATA_HOME`
  - `~/.local/share` = `stdpath('data')`
  - `/usr/local/share`
  - `/usr/share`

:::

위 경로에서 다음 스크립트들이 있는지 확인합니다.

- filetype.lua
- autoload/
- colors/
- compiler/
- doc/
- ftplugin/
- indent/
- keymap/
- lang/
- lsp/
- lua/
- pack/
  - Neovim 0.12+ supports `vim.pack.add`
- parser/
- plugin/
- queries/
- rplugin/
- spell/
- syntax/
- tutor/

## Options

- [Options](https://neovim.io/doc/user/options.html)
- [Eval](https://neovim.io/doc/user/eval.html)
- [Neovim Lua](https://neovim.io/doc/user/lua.html)

<br />

- `vim.g.*`: [global-variable](https://neovim.io/doc/user/eval.html#global-variable)
- `vim.opt.*`: [vim.opt](https://neovim.io/doc/user/lua.html#vim.opt)
