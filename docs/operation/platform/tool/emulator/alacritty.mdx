---
id: alacritty
title: Alacritty
sidebar_label: Alacritty
description: Alacritty
keywords:
  - alacritty
ontology:
  role: operation
  domain: platform
  class: tool
  instance: emulator
  aspect: alacritty
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

## 설치

<Tabs
    groupId="os"
    defaultValue="arch"
    values={[
        {label: 'Arch Linux', value: 'arch',},
        {label: 'macOS', value: 'macos',}
    ]}
>

<TabItem value="arch">

```shell
sudo pacman -S alacritty
```

</TabItem>

<TabItem value="macos">

```shell
brew install --cask alacritty
```

</TabItem>

</Tabs>

```shell
curl -sSL https://github.com/alacritty/alacritty/releases/download/v0.15.1/alacritty.info \
    | tic -x -
```

:::info

- [SSH 환경변수 설정](/docs/operation/platform/tool/remote/ssh.mdx#ssh-환경변수-설정)

tic 명령어를 사용할 수 없는 원격 서버에 접속하는 경우 `TERM=xterm-256color ssh ...`와 같은 방식으로 접속해야합니다.
:::

## 설정

- [Alacritty Configuration](https://alacritty.org/config-alacritty.html)

### font

<Tabs
    groupId="os"
    defaultValue="arch"
    values={[
        {label: 'Arch Linux', value: 'arch',},
        {label: 'macOS', value: 'macos',}
    ]}
>

<TabItem value="arch">

```shell
sudo pacman -S ttf-d2coding-nerd
```

</TabItem>

<TabItem value="macos">

```shell
brew install --cask font-d2coding-nerd-font
```

</TabItem>

</Tabs>

```shell
fc-list

...
<path> <font>:style=<style>
...
```

```toml title="~/.config/alacritty/alacritty.toml"
[font]
normal = { family = "D2CodingLigature Nerd Font", style = "Regular" }
size = 13
```

### window

```toml title="~/.config/alacritty/alacritty.toml"
[window]
dimensions = { columns = 120, lines = 50 }
padding = { x = 5, y = 5 }
option_as_alt = "Both" # macOS
```

### theme

- https://github.com/alacritty/alacritty-theme

```shell
# We use Alacritty's default Linux config directory as our storage location here.
mkdir -p ~/.config/alacritty/themes
git clone https://github.com/alacritty/alacritty-theme ~/.config/alacritty/themes
```

```toml title="~/.config/alacritty/alacritty.toml"
[general]
import = [
  "~/.config/alacritty/themes/themes/github_dark.toml",
]
```
