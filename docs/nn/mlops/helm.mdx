---
id: helm
title: Helm
sidebar_label: Helm
description: Helm
keywords:
  - helm
---

## Installation

```shell
sudo snap install helm --classic
```

## Repository

```shell
helm repo add <NAME> <URL> [flags]
```

Ex) `helm repo add stable https://charts.helm.sh/stable`

```shell
helm repo list [flags]
```

```shell
helm search repo <keyword> [flags]
```

```shell
helm repo update [REPO1 [REPO2 ...]] [flags]
```

## Chart

### Installation

```shell
helm show values <CHART> [flags]
```

```shell
helm template <NAME> <CHART> [flags] > <NAME>.yaml
```

:::info
`helm template`을 사용해서 yaml 파일을 생성하고, `kustomize`를 사용해서 설치하는 것을 고려해 볼 수 있습니다.
:::

```shell
helm install <NAME> <CHART> [flags]
```

- `-f <path>`: 오버라이드 할 YAML 설정 파일
- `--set <name>=<value>`: 오버라이드 할 설정

```shell
helm list [flags]
```

```shell
helm status <NAME> [flags]
```

### Management

```shell
helm upgrade <NAME> <CHART> [flags]
```

```shell
helm get values <CHART> [flags]
```

```shell
helm get manifests <NAME> [flags]
```

```shell
helm history <NAME> [flags]
```

```shell
helm rollback <NAME> <REVISION> [flags]
```

### Uninstallation

```shell
helm uninstall <NAME> [...] [flags]
```
