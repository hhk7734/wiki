---
id: cluster
title: "kind 클러스터 생성 가이드"
sidebar_label: "클러스터 생성 가이드"
description: "kind 클러스터 생성 가이드 documents cluster material for kind in the infrastructure domain. It is classified as an operation page for the cluster-orchestrator class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights 설치, 클러스터 생성, and 클러스터 삭제, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include kubernetes, cluster, kind 클러스터 생성 가이드, and infrastructure, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / kubernetes / kind / cluster, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - kind
  - kubernetes
  - cluster
  - "kind 클러스터 생성 가이드"
  - infrastructure
ontology:
  role: operation
  domain: infrastructure
  class: cluster-orchestrator
  instance: kind
  aspect: cluster
subject:
  canonical_name: kind
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## 설치

:::info[References]

- [mise로 런타임 버전 관리하기](/docs/system/mise/overview.mdx#버전-관리)

:::

```shell
mise use kind
```

## 클러스터 생성

:::info[References]

- [kind / User Guide / Quick Start](https://kind.sigs.k8s.io/docs/user/quick-start/)
- [kind / User Guide / Configuration](https://kind.sigs.k8s.io/docs/user/configuration/)

:::

```shell
kind create cluster [<flags>]
```

- `--name <clusterName>`
- `--config <configFilePath>`

```yaml title="config.yaml"
apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
```

- `nodes`
  - `role: control-plane|worker`
  - `image: kindest/node:<version>[@sha256:<digest>]`
    - [kind GitHub / Releases](https://github.com/kubernetes-sigs/kind/releases)

## 클러스터 삭제

```shell
kind delete cluster [<flags>]
```

- `--name <clusterName>`
