---
id: auth
title: "Kubespray 인증/인가 변경"
sidebar_label: "인증/인가 변경"
description: "Kubespray 인증/인가 변경 documents auth material for Kubespray in the infrastructure domain. It is classified as an operation page for the cluster-orchestrator class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights kube-apiserver 인증서에 SAN 추가, and OIDC 설정, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include cert, authn, authz, kubespray 인증/인가 변경, and auth, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / kubernetes / kubespray / auth, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - kubespray
  - cert
  - authn
  - authz
  - "kubespray 인증/인가 변경"
  - auth
  - infrastructure
  - kubernetes
ontology:
  role: operation
  domain: infrastructure
  class: cluster-orchestrator
  instance: kubespray
  aspect: auth
subject:
  canonical_name: Kubespray
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## kube-apiserver 인증서에 SAN 추가

[HA / 사용자 정의 네트워크](/docs/infrastructure/kubernetes/kubespray/ha.mdx#사용자-정의-네트워크)

## OIDC 설정

```yaml title="group_vars/k8s_cluster/k8s-cluster.yml"
kube_oidc_auth: true

kube_oidc_url: ""
kube_oidc_client_id: ""
# kube_oidc_ca_file: "{{ kube_cert_dir }}/ca.pem"
kube_oidc_username_claim: email
# kube_oidc_username_prefix: 'oidc:'
kube_oidc_groups_claim: groups
# kube_oidc_groups_prefix: 'oidc:'
```

```shell
ansible-playbook -i <inventory> -b upgrade-cluster.yml --tags master
```

클라이언트 설정은 [Kubernetes 인증(Authn)](/docs/infrastructure/security/authn/overview.mdx)을 참고하세요.

:::info

`/etc/kubernetes/kubeadm-config.yaml`에 OIDC 설정이 반영되었지만 kube-apiserver에 옵션이 추가되지 않은 경우 kube_control_plane 노드에 접속하여 아래 명령어를 실행합니다.

```shell
kubeadm init phase control-plane apiserver --config=/etc/kubernetes/kubeadm-config.yaml
```

:::
