---
id: bootstrap-k0smotron
title: "Cluster API에서 k0smotron BootstrapProvider 사용하기"
sidebar_label: k0smotron
description: "Cluster API에서 k0smotron BootstrapProvider 사용하기 documents bootstrap k0smotron material for Cluster API in the infrastructure domain. It is classified as an operation page for the cluster-orchestrator class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights BootstrapProvider, K0sWorkerConfig, and K0sWorkerConfigTemplate, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include cluster-api, bootstrap, provider, k0smotron, and cluster api에서 k0smotron bootstrapprovider 사용하기, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / kubernetes / cluster-api / bootstrap-k0smotron, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation."
keywords:
  - cluster-api
  - bootstrap
  - provider
  - k0smotron
  - "cluster api"
  - "cluster api에서 k0smotron bootstrapprovider 사용하기"
  - bootstrap-k0smotron
  - infrastructure
  - kubernetes
ontology:
  role: operation
  domain: infrastructure
  class: cluster-orchestrator
  instance: cluster-api
  aspect: bootstrap-k0smotron
subject:
  canonical_name: "Cluster API"
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## BootstrapProvider

```yaml
---
apiVersion: v1
kind: Namespace
metadata:
  name: capi-k0smotron-bootstrap

---
apiVersion: operator.cluster.x-k8s.io/v1alpha2
kind: BootstrapProvider
metadata:
  name: k0sproject-k0smotron
  namespace: capi-k0smotron-bootstrap
spec:
  version: v1.7.0
```

- `version: <version>`
  - [k0smotron GitHub / Releases](https://github.com/k0sproject/k0smotron/releases)

## K0sWorkerConfig

:::info[References]

- [k0smotron Docs / Reference / bootstrap # K0sWorkerConfig.spec](https://docs.k0smotron.io/stable/resource-reference/bootstrap.cluster.x-k8s.io-v1beta1/#k0sworkerconfigspec)

:::

```yaml
apiVersion: bootstrap.cluster.x-k8s.io/v1beta1
kind: K0sWorkerConfig
spec:
  version: v1.32.6
  useSystemHostname: true
```

- `version: <version>`
  - [k0s GitHub / Releases](https://github.com/k0sproject/k0s/releases)
- `useSystemHostname: false`
  - `true`로 설정한 경우 Node 이름이 `hostname` 명령어의 출력값으로 설정됩니다.
  - `false`로 설정한 경우 Machine의 이름으로 설정됩니다.
- `args: []`
  - `k0s worker` 명령어에 추가로 전달할 인자를 설정합니다.
  - [k0s # worker ](/docs/infrastructure/kubernetes/k0s/install.mdx#worker)

## K0sWorkerConfigTemplate

:::info[References]

- [k0smotron Docs / Reference / bootstrap # K0sWorkerConfigTemplate.spec.template](https://docs.k0smotron.io/stable/resource-reference/bootstrap.cluster.x-k8s.io-v1beta1/#k0sworkerconfigtemplatespectemplate)

:::

```yaml
apiVersion: bootstrap.cluster.x-k8s.io/v1beta1
kind: K0sWorkerConfigTemplate
spec:
  template:
    spec: {}
```

- `spec`
  - `template`
    - `spec`
      - [#K0sWorkerConfig](#k0sworkerconfig)
