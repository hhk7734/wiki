---
id: install
title: "KServe 설치 가이드"
sidebar_label: "설치 가이드"
description: "KServe 설치 가이드 documents installation material for KServe in the infrastructure domain. It is classified as an operation page for the ml-platform class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights 설치, and Test, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include serverless, kserve 설치 가이드, install, infrastructure, and ai-platform, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / ai-platform / kserve / install, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - kserve
  - serverless
  - "kserve 설치 가이드"
  - install
  - infrastructure
  - ai-platform
ontology:
  role: operation
  domain: infrastructure
  class: ml-platform
  instance: kserve
  aspect: install
subject:
  canonical_name: KServe
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

:::info
[KServe 개요](/docs/infrastructure/ai-platform/kserve/overview.mdx) 문서에서 control plane과 data plane 구성을 먼저 확인할 수 있습니다.
:::

## 설치

- https://github.com/kserve/kserve/releases

```shell
helm upgrade -i kserve-crd kserve-crd-v0.11.2.tgz \
  --history-max 5
```

```shell
helm show values kserve-v0.11.2.tgz \
  > kserve-values.yaml
```

```yaml title="kserve-values.yaml"
kserve:
  controller:
    gateway:
      domain: <domain>
      localGateway:
        # InferenceService를 생성하면
        # gateway로 들어오는 요청을 gatewayService:80으로 전달하는
        # VirtualService를 생성합니다
        gateway: <gatewayNamespace>/<gatewayName>
        gatewayService: <ingressGatewayServiceHost>
      ingressGateway:
        gateway: <gatewayNamespace>/<gatewayName>
        gatewayService: <ingressGatewayServiceHost>
  modelmesh:
    enabled: false
```

```shell
helm template kserve kserve-v0.11.2.tgz \
  -n kserve \
  -f kserve-values.yaml \
  > kserve.yaml
```

```shell
helm upgrade -i kserve kserve-v0.11.2.tgz \
  --history-max 5 \
  -n kserve \
  -f kserve-values.yaml
```

## Test

```shell
kubectl create namespace kserve-test
```

```yaml
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: sklearn-iris
  namespace: kserve-test
spec:
  predictor:
    model:
      modelFormat:
        name: sklearn
      storageUri: gs://kfserving-examples/models/sklearn/1.0/model
      args: ["--enable_docs_url=True"]
```

```shell
curl -v \
  -H "Host: sklearn-iris.kserve-test.svc.cluster.local" \
  http://localhost:8081/v1/models/sklearn-iris:predict \
  -d '{"instances": [[6.8,  2.8,  4.8,  1.4],[6.0,  3.4,  4.5,  1.6]]}'
```
