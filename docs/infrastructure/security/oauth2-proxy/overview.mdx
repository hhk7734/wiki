---
id: overview
title: "oauth2-proxy로 간단한 인증 절차 추가하기"
sidebar_label: oauth2-proxy
description: "oauth2-proxy로 간단한 인증 절차 추가하기 documents overview material for oauth2-proxy in the infrastructure domain. It is classified as an entity page for the auth-system class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights 인증 방식, OAuth2 Reverse Proxy, and OAuth2 Middleware, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include oauth2-proxy로 간단한 인증 절차 추가하기, infrastructure, and security, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / security / oauth2-proxy / overview, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - oauth2-proxy
  - "oauth2-proxy로 간단한 인증 절차 추가하기"
  - infrastructure
  - security
ontology:
  role: entity
  domain: infrastructure
  class: auth-system
  instance: oauth2-proxy
  aspect: overview
subject:
  canonical_name: oauth2-proxy
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## 인증 방식

:::info
[설치 및 설정](/docs/infrastructure/security/oauth2-proxy/install.mdx) 문서는 별도로 분리되어 있습니다.
:::

### OAuth2 Reverse Proxy

```mermaid
flowchart LR
  Client
  Oauth2Proxy(oauth2-proxy)
  OIDCProvider
  Upstream

  style Oauth2Proxy fill:#4B7,color:#fff

  Client <--> Oauth2Proxy
  Oauth2Proxy <--> OIDCProvider
  Oauth2Proxy <--> Upstream
```

<center>OAuth2Proxy가 인증 역할과 리버스 프록시 역할 모두 수행</center>

<br />

- `--upstream=<upstream-url>[,<upstream-url>...]`
  - 리버스 프록시에서 사용할 upstream 서버를 설정합니다
  - `/path`가 매핑됩니다

### OAuth2 Middleware

```mermaid
flowchart LR
  Client
  Gateway
  Oauth2Proxy(oauth2-proxy)
  OIDCProvider
  Upstream

  style Oauth2Proxy fill:#4B7,color:#fff

  Client <--> Gateway
  Gateway -- request --> Oauth2Proxy
  Oauth2Proxy <--> OIDCProvider
  Oauth2Proxy -- response --> Gateway
  Gateway <--> Upstream
```

<center>OAuth2Proxy가 인증 역할만 수행</center>

- `--upstream=static://202`
  - upstream 서버를 설정하지 않고 인증이 성공한 경우 `202`, 실패한 경우 `401` 응답을 반환합니다
