---
id: ext-authz
title: "EnvoyFilter로 외부 인증서버와 통신하기"
sidebar_label: "외부 인증서버"
description: "EnvoyFilter로 외부 인증서버와 통신하기 documents ext authz material for Istio in the infrastructure domain. It is classified as an operation page for the service-mesh class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights ExtAuthz, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include envoyfilter, ext-authz, envoyfilter로 외부 인증서버와 통신하기, infrastructure, and networking, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / networking / istio / ext-authz, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - envoyfilter
  - ext-authz
  - istio
  - "envoyfilter로 외부 인증서버와 통신하기"
  - infrastructure
  - networking
ontology:
  role: operation
  domain: infrastructure
  class: service-mesh
  instance: istio
  aspect: ext-authz
subject:
  canonical_name: Istio
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## ExtAuthz

:::info[References]

- [Envoy / Configuration / HTTP / Filters / ExtAuthz](https://www.envoyproxy.io/docs/envoy/latest/configuration/http/http_filters/ext_authz_filter)
- [Envoy / API / V3 / Extensions / Filters / HTTP / ExtAuthz](https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/filters/http/ext_authz/v3/ext_authz.proto)

:::

```yaml
spec:
  configPatches:
    - applyTo: HTTP_FILTER
      match:
        context: GATEWAY
        listener:
          filterChain:
            filter:
              name: envoy.filters.network.http_connection_manager
      patch:
        operation: INSERT_BEFORE
        value:
          name: envoy.filters.http.ext_authz
          typed_config:
            "@type": type.googleapis.com/envoy.extensions.filters.http.ext_authz.v3.ExtAuthz
            # status_on_error:
            #   code: ServiceUnavailable # 문제가 생기면 503으로 응답
            http_service:
              server_uri:
                uri: http://<service>.<namespace>.svc.cluster.local
                cluster: outbound|8080||<service>.<namespace>.svc.cluster.local
                timeout: 10s
              authorization_request:
                allowed_headers:
                  patterns:
                    - exact: cookie
                    - exact: authorization
                    - exact: x-auth-token
              authorization_response:
                allowed_upstream_headers:
                  patterns:
                    - exact: x-auth-request-email
                    - exact: authorization
    - applyTo: HTTP_ROUTE
      match:
        routeConfiguration:
          vhost:
            name: kubeflow.loliot.net:80
            route:
              name: <.VirtualService.spec.http.name> # 특정한 이름의 라우팅에 대해 적용
      patch:
        operation: MERGE
        value:
          typed_per_filter_config:
            envoy.filters.http.ext_authz:
              "@type": type.googleapis.com/envoy.extensions.filters.http.ext_authz.v3.ExtAuthzPerRoute
              disabled: true
```
