---
id: gateway
title: "Gateway API로 라우팅 관리하기"
sidebar_label: "라우팅 관리하기"
description: "Gateway API로 라우팅 관리하기 documents gateway material for Gateway API in the infrastructure domain. It is classified as an entity page for the gateway-api class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights Gateway, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include gateway, istio, gateway api로 라우팅 관리하기, gateway-api, and infrastructure, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / networking / gateway-api / gateway, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - gateway
  - istio
  - "gateway api"
  - "gateway api로 라우팅 관리하기"
  - gateway-api
  - infrastructure
  - networking
ontology:
  role: entity
  domain: infrastructure
  class: gateway-api
  instance: gateway-api
  aspect: gateway
subject:
  canonical_name: "Gateway API"
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: supporting
  confidence: exact
---

## Gateway

:::info[References]

- [Gateway API로 라우팅 관리하기 # Gateway](/docs/infrastructure/networking/gateway-api/route.mdx#gateway)
- [Istio / Kubernetes Gateway API # Automated deployment](https://istio.io/latest/docs/tasks/traffic-management/ingress/gateway-api/#automated-deployment)

:::

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: ingress-infrastructure
  namespace: istio-system
data:
  service: |
    spec:
      type: LoadBalancer
      externalTrafficPolicy: "Local"

  # serviceAccount: |

  deployment: |
    spec:
      replicas: 3
      template:
        spec:
          containers:
            - name: istio-proxy
              resources:
                requests:
                  cpu: 500m
                  memory: 512Mi
                limits:
                  cpu: "2"
                  memory: 1Gi
          affinity:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: topology.kubernetes.io/zone
                        operator: In
                        values:
                          - ap-northeast-2a
                          - ap-northeast-2c
            podAntiAffinity:
              preferredDuringSchedulingIgnoredDuringExecution:
                - weight: 50
                  podAffinityTerm:
                    topologyKey: kubernetes.io/hostname
                    labelSelector:
                      matchExpressions: []
                    matchLabelKeys:
                      - gateway.networking.k8s.io/gateway-name
                      - pod-template-hash

  horizontalPodAutoscaler: |
    spec:
      minReplicas: 3
      maxReplicas: 5

  podDisruptionBudget: |
    spec:
      minAvailable: 1
```

```yaml
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: ingress
  namespace: istio-system
spec:
  gatewayClassName: istio
  infrastructure:
    parametersRef:
      group: ""
      kind: ConfigMap
      name: ingress-infrastructure
```
