---
id: crd
title: "Argo Workflows CRD"
sidebar_label: CRD
description: "Argo Workflows CRD documents custom resource definitions material for Argo Workflows in the infrastructure domain. It is classified as an operation page for the workflow-system class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights Workflow and WorkflowTemplate, entrypoint and templates, Variables, podGC, and ttlStrategy, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include argo, workflows, crd, argo workflows crd, and argo-workflows, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / workflow / argo-workflows / crd, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation."
keywords:
  - argo
  - workflows
  - crd
  - "argo workflows"
  - "argo workflows crd"
  - argo-workflows
  - infrastructure
  - workflow
ontology:
  role: operation
  domain: infrastructure
  class: workflow-system
  instance: argo-workflows
  aspect: crd
subject:
  canonical_name: "Argo Workflows"
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## Workflow and WorkflowTemplate

- https://argoproj.github.io/argo-workflows/fields/#workflowspec

```yaml
apiVersion: argoproj.io/v1alpha1
kind: Workflow # Workflow|WorkflowTemplate|ClusterWorkflowTemplate
metadata:
  generateName: hello-world-
spec:
  entrypoint: <templateName> # 처음 시작되어야 할 템플릿 이름
  templates:
    - <template>
```

- `serviceAccountName`: workflow에 의해 실행되는 Pod의 ServiceAccount

:::info
Workflow와 WorkflowTemplate는 `.status` 필드만 다릅니다.
:::

### `entrypoint` and `templates`

- [Argo Workflows entrypoint and templates](/docs/infrastructure/workflow/argo-workflows/entrypoint-templates.mdx)

### Variables

- [Argo Workflows Variables](/docs/infrastructure/workflow/argo-workflows/variables.mdx)

### `podGC`

- [podGC](/docs/infrastructure/workflow/argo-workflows/garbage-collection.mdx#podgc)

### `ttlStrategy`

- [ttlStrategy](/docs/infrastructure/workflow/argo-workflows/garbage-collection.mdx#ttlstrategy)

### `volumes`

`container`가 마운트할 수 있는 볼륨 목록을 선언할 수 있습니다.

- https://argoproj.github.io/argo-workflows/fields/#volume

```yaml
spec:
  volumes:
    - name: <volumeName>
      persistentVolumeClaim:
        claimName: <existingPvcName>

  templates:
    - name: <templateName>
      container:
        image: ubuntu:20.04
        command:
          - ls
        args:
          - /mnt/vol
        volumeMounts:
          - name: <volumeName>
            mountPath: /mnt/vol
```

```yaml
spec:
  volumeClaimGC:
    strategy: OnWorkflowSuccess # OnWorkflowSuccess | OnWorkflowCompletion
  volumeClaimTemplates:
    - metadata:
        name: <pvcSuffix> # PersistentVolumeClaim 이름은 <workflowName>-<pvcSuffix> 형태로 생성됩니다.
      spec:
        accessModes: [ReadWriteOnce]
        resources:
          requests:
            storage: 1Gi

  templates:
    - name: <templateName>
      container:
        image: ubuntu:20.04
        command:
          - ls
        args:
          - /mnt/vol
        volumeMounts:
          - name: <pvcSuffix>
            mountPath: /mnt/vol
```

## CronWorkflow

- https://argoproj.github.io/argo-workflows/fields/#cronworkflowspec

```yaml
apiVersion: argoproj.io/v1alpha1
kind: CronWorkflow
metadata:
  name: <name>
  namespace: <namespace>
spec:
  schedule: "* * * * *" # https://en.wikipedia.org/wiki/Cron
  timezone: Asia/Seoul
  concurrencyPolicy: "Allow" # Allow|Forbid|Replace, CronJob.spec.concurrencyPolicy
  startingDeadlineSeconds: 0 # CronJob.spec.startingDeadlineSeconds
  suspend: false # Cron을 멈춰두고 싶은 경우 true로 설정하면 됩니다.
  # workflowMetadata:
  #   labes: {}
  #   annotations: {}
  workflowSpec: {} # Workflow.spec
```

## Reference

- https://argoproj.github.io/argo-workflows/workflow-concepts/
- https://argoproj.github.io/argo-workflows/fields/
