---
id: etc
title: etc
sidebar_label: etc
description: "etc documents etc material for AWS ParallelCluster in the infrastructure domain. It is classified as an operation page for the cloud-service class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights Instance 설정, Environment, ResumeTimeout, scontrol 명령어, and CF 상태에서 멈춰있는 작업 재시작하기, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include parallelcluster, etc, parallel-cluster, infrastructure, and cloud, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / cloud / parallel-cluster / etc, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - parallelcluster
  - "aws parallelcluster"
  - etc
  - parallel-cluster
  - infrastructure
  - cloud
ontology:
  role: operation
  domain: infrastructure
  class: cloud-service
  instance: parallel-cluster
  aspect: etc
subject:
  canonical_name: "AWS ParallelCluster"
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## Instance 설정

### Environment

- `/opt/slurm`
- `/opt/slurm/etc/slurm.conf`
- `/opt/slurm/etc/slurm.sh`
- `/etc/profile.d/slurm.sh -> /opt/slurm/etc/slurm.sh`(symbolic link)

#### ResumeTimeout

`/opt/slurm/etc/slurm.conf` 파일에서 아래 설정값을 수정하여 잡이 없을 때 컴퓨트 노드가 꺼질 시간을 설정합니다.

```shell
ResumeTimeout=600
```

## scontrol 명령어

`<jobid>`, `<jobid>,<jobid>`, `{<jobid>..<jobid>}`

### CF 상태에서 멈춰있는 작업 재시작하기

Suspend 상태로 만들기

```shell
sudo `which scontrol` suspend jobid=<jobid>
```

Suspend -> Pending 상태로 만들기

```shell
sudo `which scontrol` requeue jobid=<jobid>
```

Pending -> Configuring or Running 상태로 만들기

```shell
sudo `which scontrol` requeue jobid=<jobid>
```
