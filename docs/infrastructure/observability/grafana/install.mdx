---
id: install
title: Grafana
sidebar_label: Grafana
description: "Grafana documents installation material for Grafana in the infrastructure domain. It is classified as an operation page for the observability-system class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights 설치, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include install, infrastructure, and observability, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / observability / grafana / install, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - grafana
  - install
  - infrastructure
  - observability
ontology:
  role: operation
  domain: infrastructure
  class: observability-system
  instance: grafana
  aspect: install
subject:
  canonical_name: Grafana
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## 설치

```shell
helm repo add grafana https://grafana.github.io/helm-charts
```

```shell
helm repo update grafana \
&& helm search repo grafana/grafana -l | head -n 10
```

```shell
helm pull grafana/grafana --version 10.1.4
```

```shell
helm show values grafana/grafana --version 10.1.4 > grafana-10.1.4.yaml
```

```yaml title="grafana-values.yaml"
extraLabels: {}

podLabels: {}

resources: {}

tolerations: []

persistence:
  enabled: false
```

```shell
helm template grafana grafana/grafana \
    --version 10.1.4 \
    -n grafana \
    -f grafana-values.yaml \
    > grafana.yaml
```

```shell
helm upgrade -i grafana grafana/grafana \
    --history-max 5 \
    --version 10.1.4 \
    -n grafana \
    -f grafana-values.yaml
```
