---
id: index
title: metrics-server
sidebar_label: metrics-server
description: "metrics-server documents installation material for metrics-server in the infrastructure domain. It is classified as an operation page for the observability-system class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights metrics server, 설치, 삭제, and Troubleshooting, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include install, infrastructure, and observability, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / observability / metrics-server / install / index, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - metrics-server
  - install
  - infrastructure
  - observability
ontology:
  role: operation
  domain: infrastructure
  class: observability-system
  instance: metrics-server
  aspect: install
subject:
  canonical_name: metrics-server
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

import Image from "@theme/IdealImage";

## metrics server

<center>
	<Image img={require("./metrics-server.png")} width={750} />
</center>
<center>https://ssup2.github.io/theory_analysis/Kubernetes_Metric_Server/</center>
<br />

## 설치

:::info[References]

- [metrics-server](https://kubernetes-sigs.github.io/metrics-server/)

:::

```shell
helm repo add metrics-server https://kubernetes-sigs.github.io/metrics-server/
```

```shell
helm repo update metrics-server \
&& helm search repo metrics-server/metrics-server -l | head -n 10
```

```shell
helm pull metrics-server/metrics-server --version 3.12.2
```

```shell
helm show values metrics-server/metrics-server --version 3.12.2 \
    > metrics-server-3.12.2.yaml
```

```yaml title="metrics-server-values.yaml"
commonLabels: {}

args: []
  # - --kubelet-insecure-tls
  # - --v=2

resources:
  requests:
    cpu: 100m
    memory: 200Mi

affinity: {}

tolerations: []
```

- `affinity: {}`
  - [Affinity](/docs/infrastructure/kubernetes/affinity.mdx)
- `tolerations: []`
  - [Taints and Tolerations](/docs/infrastructure/kubernetes/taints-and-tolerations.mdx)

:::info
로그에 `"Failed to scrape node" err="Get \"https://xxx:10250/stats/summary?only_cpu_and_memory=true\": x509: cannot validate certificate for xxx because it doesn't contain any IP SANs" node="xxx"`와 같은 에러가 발생하는 경우 `--kubelet-insecure-tls`를 추가해주세요.
:::

```shell
helm template metrics-server metrics-server/metrics-server \
    --version 3.12.2 \
    -n kube-system \
    -f metrics-server-values.yaml \
    > metrics-server.yaml
```

```shell
helm upgrade -i metrics-server metrics-server/metrics-server \
    --history-max 5 \
    --version 3.12.2 \
    -n kube-system \
    -f metrics-server-values.yaml
```

## 삭제

```shell
helm uninstall metrics-server -n kube-system
```

## Troubleshooting

```shell
kubectl describe apiservices v1beta1.metrics.k8s.io
```
