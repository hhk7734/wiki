---
id: install
title: Jaeger
sidebar_label: Jaeger
description: "Jaeger documents installation material for Jaeger in the infrastructure domain. It is classified as an operation page for the service-mesh class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights 설치, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include istio, install, infrastructure, and observability, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / observability / jaeger / install, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - jaeger
  - istio
  - install
  - infrastructure
  - observability
ontology:
  role: operation
  domain: infrastructure
  class: service-mesh
  instance: jaeger
  aspect: install
subject:
  canonical_name: Jaeger
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## 설치

```shell
helm repo add jaegertracing https://jaegertracing.github.io/helm-charts
```

```shell
helm repo update jaegertracing \
&& helm search repo jaegertracing/jaeger-operator -l | head -n 10
```

```shell
helm show values jaegertracing/jaeger-operator \
    --version 2.38.0 \
    > jaeger-operator-values.yaml
```

```yaml title="jaeger-operator-values.yaml"

```

```shell
helm template jaeger-operator jaegertracing/jaeger-operator \
    --version 2.38.0 \
    -n monitoring \
    -f jaeger-operator-values.yaml \
    > jaeger-operator.yaml
```

```shell
helm upgrade -i jaeger-operator jaegertracing/jaeger-operator \
    --version 2.38.0 \
    -n monitoring \
    --history-max 5 \
    -f jaeger-operator-values.yaml
```
