---
id: vault
title: "Ansible Vault를 사용하여 Secret 관리하기"
sidebar_label: Vault
description: "Ansible Vault를 사용하여 Secret 관리하기 documents vault material for Ansible in the infrastructure domain. It is classified as an operation page for the workflow-system class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights Variable, and Vault가 사용된 Playbook 실행, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include vault, ansible vault를 사용하여 secret 관리하기, infrastructure, and iac, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / iac / ansible / vault, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - ansible
  - vault
  - "ansible vault를 사용하여 secret 관리하기"
  - infrastructure
  - iac
ontology:
  role: operation
  domain: infrastructure
  class: workflow-system
  instance: ansible
  aspect: vault
subject:
  canonical_name: Ansible
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## Variable

```shell
ansible-vault encrypt_string --vault-id [<user>]<passwordMethod> <flags> [value]
```

- `--vault-id [<user>]<passwordMethod>`
  - `@prompt`, `<user>@prompt`: 터미널에서 password를 입력받습니다.
  - `<filePath>`, `<user>@<filePath>`: 파일에서 password를 읽어옵니다.
  - `ANSIBLE_VAULT_IDENTITY_LIST=[]`: `--vault-id` 기본 값입니다.
  - `ANSIBLE_VAULT_IDENTITY=default`: `<user>` 기본 값입니다.
  - `ANSIBLE_VAULT_PASSWORD_FILE=`: password 파일 기본 값입니다.
- `<flags>`
  - `--name <key>`: key를 지정합니다. 설정하지 않으면 터미널에서 입력받습니다.

## Vault가 사용된 Playbook 실행

```shell
ansible-playbook --vault-id [<user>]@<passwordMethod> -i <inventory> <playbook>
```
