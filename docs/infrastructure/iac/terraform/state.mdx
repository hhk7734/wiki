---
id: state
title: "Terraform State 수정하기"
sidebar_label: "State 수정하기"
description: "Terraform State 수정하기 documents state material for Terraform in the infrastructure domain. It is classified as an operation page for the iac-tool class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights tfstate 파일 직접 수정, state mv, and 리소스 이름 바꾸기, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include state, terraform state 수정하기, infrastructure, and iac, helping both humans and generated indexes connect this page with adjacent concepts. The route is infrastructure / iac / terraform / state, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - terraform
  - state
  - "terraform state 수정하기"
  - infrastructure
  - iac
ontology:
  role: operation
  domain: infrastructure
  class: iac-tool
  instance: terraform
  aspect: state
subject:
  canonical_name: Terraform
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## tfstate 파일 직접 수정

```shell
terraform state pull > terraform.tfstate
```

`terraform.tfstate` 파일은 JSON 형식으로 되어 있습니다. 수정이 필요한 부분을 수정한 후, `.serial` 값을 증가시켜줍니다.

```shell
terraform state push terraform.tfstate
```

## `state mv`

### 리소스 이름 바꾸기

```shell
terraform state mv <resourceType>.<resourceName> <resourceType>.<newResourceName>
```

변경이 완료되면 리소스 선언과 리소스를 변수로 사용하고 있는 부분을 찾아서 모두 수정해야 합니다.
