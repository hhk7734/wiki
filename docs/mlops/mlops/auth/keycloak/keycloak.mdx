---
id: keycloak
title: keycloak
sidebar_label: keycloak
description: keycloak
keywords:
  - keycloak
---

## 설치

```shell
helm repo add bitnami https://charts.bitnami.com/bitnami
```

```shell
helm repo update bitnami \
&& helm search repo bitnami/keycloak -l | head -n 10
```

```shell
helm pull bitnami/keycloak \
    --version 13.3.0
```

```shell
helm show values keycloak-13.3.0.tgz \
    > keycloak-values.yaml
```

```yaml title="keycloak-values.yaml"
commonLabels: {}

auth:
  adminUser: <user>
  adminPassword: <password>

resources:
  requests:
    cpu: 250m
    memory: 1Gi
  limits:
    cpu: 250m
    memory: 1Gi

podLabels: {}

service:
  type: ClusterIP

postgresql:
  enabled: false

externalDatabase:
  host: <host>
  port: <port>
  user: <user>
  password: <password>
  database: <database>

cache:
  enabled: false
```

```shell
helm template keycloak keycloak-13.3.0.tgz \
    -n auth \
    -f keycloak-values.yaml \
    > keycloak.yaml
```

```shell
helm upgrade keycloak keycloak-13.3.0.tgz \
    --install \
    --history-max 3 \
    -n auth \
    -f keycloak-values.yaml
```

## Upgrade

- https://www.keycloak.org/docs/latest/upgrading/#migration-changes
