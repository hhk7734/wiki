---
id: install
title: "RDMA Shared Device Plugin 설치 가이드"
sidebar_label: "설치 가이드"
description: "RDMA Shared Device Plugin 설치 가이드"
keywords:
  - rdma
  - "shared device plugin"
  - cdi
ontology:
  role: operation
  domain: mlops
  class: networking-stack
  instance: rdma-shared-device-plugin
  aspect: install
subject:
  canonical_name: "RDMA Shared Device Plugin"
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

:::info
[RDMA Shared Device Plugin 개요](/docs/mlops/rdma-shared-device-plugin/overview.mdx) 문서에서 플러그인 역할과 설정 예시를 먼저 확인할 수 있습니다.
:::

## 설치

```shell
helm repo add lol-iot https://lol-iot.github.io/helm-charts
```

```shell
helm repo update lol-iot \
&& helm search repo lol-iot/rdma-shared-device-plugin -l | head -n 10
```

```shell
helm pull lol-iot/rdma-shared-device-plugin --version 1.5.3
```

```shell
helm show values lol-iot/rdma-shared-device-plugin --version 1.5.3 \
  > rdma-searched-device-plugin-1.5.3.yaml
```

```shell
helm template rdma-shared-device-plugin lol-iot/rdma-shared-device-plugin \
    --version 1.5.3 \
    -n kube-system \
    -f rdma-searched-device-plugin-values.yaml \
    > rdma-shared-device-plugin.yaml
```

```shell
helm upgrade -i rdma-shared-device-plugin lol-iot/rdma-shared-device-plugin \
    --history-max 5 \
    --version 1.5.3 \
    -n kube-system \
    -f rdma-searched-device-plugin-values.yaml
```
