---
id: derivative
title: Derivative
sidebar_label: Derivative
description: "Derivative documents derivative material for Neural Network in the science domain. It is classified as an operation page for the model-family class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights Loss function, Mean Square Error, Binary Cross-Entropy, Activation, and logistic, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include derivative, nn, and science, helping both humans and generated indexes connect this page with adjacent concepts. The route is science / nn / derivative, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - "neural network"
  - derivative
  - nn
  - science
ontology:
  role: operation
  domain: science
  class: model-family
  instance: nn
  aspect: derivative
subject:
  canonical_name: "Neural Network"
  aliases:
    - nn
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## Loss function

### Mean Square Error

$$
Loss = \dfrac{ 1 }{ 2 } \sum_i \left( y_i - a^L_i \right)^2
$$

$$
\dfrac{ \partial Loss }{ \partial a^L_i } = - \left( y_i - a^L_i \right)
$$

### Binary Cross-Entropy

$$
Loss = \sum_i \left[ -y_i \log a^L_i - (1 - y_i) \log \left( 1 - a^L_i \right) \right]
$$

$$
\dfrac{\partial Loss}{\partial a^L_i}
= \dfrac{ - \left( y_i - a^L_i \right)}{a^L_i \left( 1 - a^L_i \right) }
$$

## Activation

### logistic

$$
a^l_i = \sigma ( z^l_i ) = logistic ( z^l_i ) = \dfrac{ 1 }{ 1 + e^{ -z^l_i } }
$$

$$
\dfrac{\partial \sigma ( z^l_i )}{\partial z^l_i }
= \dfrac{  e^{ -z^l_i } }{ \left( 1 + e^{ -z^l_i } \right)^2 }
= a^l_i \left( 1 - a^l_i \right)
$$
