---
id: index
title: "OpenCL Basics"
sidebar_label: Basics
description: "OpenCL Basics documents opencl basics material for OpenCL for C++ in the language domain. It is classified as an operation page for the library class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights Platform model, Execution model, With plaform model, Context, and Command-Queues, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include c++, opencl, opencl basics, opencl-basics, and language, helping both humans and generated indexes connect this page with adjacent concepts. The route is language / cpp / opencl / opencl-basics / index, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation."
keywords:
  - "c++"
  - opencl
  - "opencl for c++"
  - "opencl basics"
  - opencl-basics
  - language
  - cpp
ontology:
  role: operation
  domain: language
  class: library
  instance: opencl
  aspect: opencl-basics
subject:
  canonical_name: "OpenCL for C++"
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---


Ref:

- [https://www.khronos.org/developers/reference-cards/](https://www.khronos.org/developers/reference-cards/)
- [http://developer.amd.com/wordpress/media/2013/12/AMD_OpenCL_Programming_User_Guide2.pdf](http://developer.amd.com/wordpress/media/2013/12/AMD_OpenCL_Programming_User_Guide2.pdf)

## Platform model

<center>
	<img src={require("./opencl-platform-model.png").default} />
</center>

## Execution model

- `kernel`: A kernel is a small unit of execution that performs a clearly defined function and that can be executed in parallel.
- `work-item`: A instance in which one kernel run.
- `work-group`: A group of work-items. Work-items grouped into a work-group are executed simultaneously.
- `NDRagne`: An index space.
- `OpenCL application`: Host program running on host + Kernels running on Compute device.

<center>
	<img src={require("./opencl-ndrange.png").default} />
</center>

In the global NDRange index space, work-item has a unique coordinate(global ID), and work-group has a work-group ID. In a local work-group index space, work-item has a unique coordinate(local ID).

Therefore, one work-item can be accessed by global ID or work-group ID + local ID.

### With plaform model

<center>
	<img src={require("./opencl-execution-model.png").default} />
</center>

### Context

<center>
	<img src={require("./opencl-context.png").default} />
</center>

### Command-Queues

Interaction between a host and an OpenCL device is caused by commands. The host uses command-queue to transmit commands to the device.

<center>
	<img src={require("./opencl-queue.jpg").default} />
</center>

## Memory model

### Hardware

<center>
	<img src={require("./opencl-memory-model.png").default} />
</center>

### Software

<center>
	<img src={require("./opencl-software-memory-model.png").default} />
</center>

## Programming models

## OpenCL information

### clinfo installation

```shell
sudo apt install -y clinfo ocl-icd-libopencl1
```

```shell
sudo mkdir -p /etc/OpenCL/vendors
```

```shell {1}
$ sudo vim /etc/OpenCL/vendors/<vendor>.icd
<path>/libOpenCL.so
```

```shell
clinfo
```
