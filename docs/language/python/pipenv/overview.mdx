---
id: overview
title: Pipenv
sidebar_label: Pipenv
description: "Pipenv documents overview material for Pipenv in the language domain. It is classified as an operation page for the environment class, so readers can understand its topic, scope, and place in the wiki taxonomy. The page highlights 설치, Virtualenv, Package, Sync, and Reference, with examples, commands, diagrams, or references kept close to the relevant explanation. Important search terms include language, and python, helping both humans and generated indexes connect this page with adjacent concepts. The route is language / python / pipenv / overview, and the frontmatter keeps the canonical subject, ontology role, and navigation context aligned with repository validation. Use it for quick orientation, implementation reminders, and deeper research."
keywords:
  - pipenv
  - language
  - python
ontology:
  role: operation
  domain: language
  class: environment
  instance: pipenv
  aspect: overview
subject:
  canonical_name: Pipenv
  aliases: []
relations:
  related_to: []
  depends_on: []
  prerequisite_for: []
  part_of: []
  implements: []
  uses: []
source:
  status: canonical
  confidence: exact
---

## 설치

```shell
python3 -m pip install -U pip \
&& python3 -m pip install pipenv
```

```shell title=".zshrc"
eval "$(_PIPENV_COMPLETE=zsh_source pipenv)"
```

## Virtualenv

```shell
pipenv --python <version>
```

해당 버전으로 가상환경이 생성됩니다. 해당 버전이 설치되어 있지않고 `pyenv`가 설치되어 있다면, 해당 버전을 설치한 후 가상환경을 생성합니다.

```shell
pipenv shell
```

가상환경이 활성화된 shell에 접속합니다.

```shell
pipenv --venv
```

가상환경 위치를 확인합니다.

```shell
pipenv --rm
```

가상환경을 삭제합니다.

## Package

```shell
pipenv install <package|git>
```

- `--dev`: 개발용 패키지인 경우 이 옵션을 사용합니다.
- `<git>`: `-e <vcs_type>+<scheme>://<location>/<user_or_organization>/<repository>@<branch_or_tag>#<package_name>`
  - `-e`: 경로를 수정할 수 있다는 의미입니다.
  - `<vcs_type>`: `git`, `bzr`, `svn` or `hg`
  - `<scheme>`: `http`, `https`, `ssh` or `file`

```shell
pipenv lock
```

`Pipfile.lock` 파일을 생성합니다.

```shell
pipenv uninstall <package>
```

- `--all`: 패키지명 대신 해당 옵션을 쓰면 모든 패키지를 제거합니다.

## Sync

`Pipfile`이 있는 경로에서 아래 명령어를 실행하면 Pipfile에 정의된 패키지가 설치됩니다.

```shell
pipenv install [--dev]
```

`Pipfile.lock`이 있는 경로에서 아래 명령어를 실행하면 Pipfile.lock에 정의된 패키지가 설치됩니다.

```shell
pipenv sync [--dev]
```

## Reference

- [https://github.com/pypa/pipenv](https://github.com/pypa/pipenv)
